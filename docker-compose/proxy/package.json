{
  "name": "dohw3-proxy",
  "version": "0.0.0",
  "description": "proxy",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.12.3",
    "redis": "^0.12.1",
    "request": "latest",
    "http-proxy": "^1.9.0"
  }
}
